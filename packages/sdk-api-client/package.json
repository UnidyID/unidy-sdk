{
  "name": "@unidy.io/sdk-api-client",
  "version": "0.1.2-alpha.0",
  "description": "Unidy API Client SDK",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "module": "dist/index.esm.js",
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "bundle": "bun build src/*.ts --outdir dist --minify --sourcemap",
    "types": "tsc",
    "build": "npm run clean && npm run bundle && npm run types"
  },
  "devDependencies": {
    "@types/events": "^3.0.3",
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@zod/mini": "^4.0.0-beta.0"
  }
}